#ifndef OPTIONS_CONFIG_H
#define OPTIONS_CONFIG_H

// CMake command options embedded from cmake_options.csv, one line per command
// Separate string literals, one per line, keep within the MSVC string-literal limit
inline constexpr const char* CMAKE_OPTIONS[] = {
@CMAKE_OPTIONS_LINES@
};

#endif

#!/bin/bash

# test framework
source $(dirname "$0")/framework_test.sh

# test
srcmlend = STDOUT
</unit>
	STDOUT

if platform.system() != "Windows" :
	dir = execute(['ls' 'dir'.split("\n")
else :
	dir = os.listdir('dir')

